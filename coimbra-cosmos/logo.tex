\documentclass{standalone}

\usepackage{fontspec}

\usepackage{tikz}
\usetikzlibrary{arrows}
\usepackage{tikz-cd}

\setmainfont[Path=/usr/share/fonts/]{Zilap Orion Urban Personal Use.ttf}
\def\logo{\tiny CO\\SM\\OS}
\begin{document}
\begin{tikzpicture}[>=stealth',scale=.5]
  \begin{scope}%[xshift=5mm]
  \foreach \i/\l in {0,...,4}{
    \coordinate (\i) at (\i*72:1);
  }
  \draw[gray!50, shorten <=2pt, shorten >=2pt, ->] (0) -- (2);
  \draw[gray!50, shorten <=2pt, shorten >=2pt, ->] (2) -- (4);
  \draw[gray!50, shorten <=2pt, shorten >=2pt, ->] (4) -- (1);
  \draw[gray!50, shorten <=2pt, shorten >=2pt, ->] (1) -- (3);
  \draw[gray!50, shorten <=2pt, shorten >=2pt, ->] (3) -- (0);
  \end{scope}
  \node at (-.5,0) {\logo};
\end{tikzpicture}
\end{document}